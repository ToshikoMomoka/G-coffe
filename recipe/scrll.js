function scrollToSection() {
    document.getElementById('postSection').scrollIntoView({
      behavior: 'smooth'
    });
  }
event.preventDefault(); 
function scrollToSection() {
    document.getElementById('contact').scrollIntoView({
      behavior: 'smooth'
    });
  }
event.preventDefault(); 